# netlify.toml (place in project root)
[build]
  command = "npm run build"
  publish = ".next"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true   # ← important: forces rewrite even if file exists